🚀 Task Workflow - Full Stack Application
Task Workflow es una solución integral para la gestión de tareas, diseñada bajo los principios de Clean Architecture en el backend y una interfaz reactiva moderna en Angular 21.

🏗️ Architecture & Design
Backend (.NET 8)
La solución está estructurada en cuatro capas para garantizar mantenibilidad y escalabilidad:

Domain: Entidades, excepciones de dominio y reglas de negocio core.

Application: Casos de uso (UseCases), DTOs, mapeos y validación con FluentValidation.

Infrastructure: Persistencia con EF Core, repositorios y configuración de SQL Server.

API: Controladores REST, Middleware global de excepciones y documentación con Swagger.

Frontend (Angular 21)
Interfaz de usuario moderna que consume la API de forma eficiente:

Signals & RxJS: Gestión de estado reactiva y manejo de flujos asíncronos.

Clean UI: Componentes modulares para la gestión visual del ciclo de vida de las tareas.

Typed Services: Integración total con los DTOs del backend mediante TypeScript.

🛠️ Business Rules & Features
Task Lifecycle
Las tareas siguen un flujo estricto validado desde el Domain:
Pending ➡️ InProgress ➡️ Completed | Blocked

Key Features
Commands: Crear, Iniciar, Completar y Bloquear/Desbloquear tareas.

Advanced Queries: * Filtrado avanzado (Estado, Título).

Paginación eficiente ejecutada a nivel de base de datos (Skip/Take).

Validation: Reglas de negocio estrictas (ej. Una tarea solo puede completarse si está InProgress).

🧪 Testing Strategy
Calidad de código asegurada mediante Unit Testing con xUnit y Moq:

Domain Tests: Validación de transiciones de estado y reglas de negocio.

Application Tests: Comportamiento de Casos de Uso, cálculos de paginación y validadores.

🚀 Running the Project
1. Backend Setup
Configurar la cadena de conexión en appsettings.json.

Ejecutar migraciones: dotnet ef database update.

Iniciar API: dotnet run.

2. Frontend Setup (Angular)
Navegar a la carpeta del cliente: cd TaskWorkflow.Client.

Instalar dependencias: npm install.

Iniciar aplicación: ng serve.
